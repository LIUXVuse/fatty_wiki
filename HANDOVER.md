## ✅ 本次完成（2026-05-19）

### 全庫斷連結修正（改名/合併遺漏）

**問題根源**：32 個概念頁改名 + ALIAS_MAP 別名合併後，來源/概念/地點頁裡的舊 `[[連結]]` 沒有同步更新，造成斷連（如 `[[龍經店]]` 應連到 `[[龍筋店]]`）。

**修正結果**：
- 掃描 來源/概念/人物/店家/地點 全部 .md
- 修正 99 個檔案、144 條連結（第一輪）
- 另修正複合詞 1 個檔案、2 條（如 `[[龍經店]]→[[龍筋店]]`）

**`update_wiki_links()` 永久強化**：
- 合併時自動推導衍生複合詞（舊名+店/按摩/服務/課 → 新名+後綴）
- 確保日後 `merge_aliases.py` 執行時不再遺漏複合詞連結

**commit 642b1ba**：101 files changed

---

## ✅ 本次完成（2026-05-19）

### merge_aliases.py 定義融合強化 + 32 個錯字概念頁改名

#### merge_aliases.py 新增 Ollama 定義融合
- 新增 `merge_definitions()`：別名頁定義 + 主名頁定義透過 Ollama 融合成一段繁中
- `merge_group()` 改為：先收集所有別名定義 → 最後呼叫 `merge_definitions()` 一次融合
- 舊名字在定義內文中自動 regex 替換為正規名
- 新增 ~25 組 ALIAS_MAP 對：武鬥派、泰洗、4P、低能量震波、舔狗、日按、日洗、Heart to Heart、客評、偷時、素股、聖水、龍宮、雞頭、清水溝、短鐘、鵝寶寶、輕功、半凹全、長鍾 等
- 端對端測試通過：定義融合結果自然流暢，集數正確合併

#### 32 個錯字概念頁批次改名
五套→無套、支男→汁男、生喉嚨→深喉嚨、色後不理→射後不理、管事服務→莞式
潛力線→前列腺、鋼交→肛交、龍津殿→龍筋店、清工→輕功、長中→長鍾
法鄉區→髮香區、罵斗→model、廣市雙馬尾→廣式雙馬尾 等 32 個

**commit b6e8815**：186 files changed

---

## ✅ 本次完成（2026-05-19）

### 概念「相關概念」段落全面填充
- 新增 `tools/enrich_related_concepts.py`（Phase 1 規則式 + Phase 2 Ollama 共現）
- **別名合併**：隆經、抓龍精、按龍機、按龍經 → 龍筋/抓龍筋；獨龍 → 毒龍；共比 → 共筆；老店 → 老點
- **Phase 1**（規則式）：95 個檔案，197 條連結
- **Phase 2**（Ollama 共現）：409 個檔案，963 條連結
- 填寫率：37/695 → **643/695**

---

## 🔴 下一個對話要先做

### Step 1：地點缺口建頁（重要）
掃描全庫發現 **1128 個連結指向不存在頁面**，其中有大量尚未建立的真實地點頁，例如：
- 台灣城市：中壢（30次）、苗栗（15次）、萬華區（15次）、中和（13次）、新竹（13次）、板橋（11次）、西門町（11次）、林森北路（11次）、彰化（9次）、三重（12次）、蘆洲（12次）、宜蘭（8次）
- 日本：沖繩（17次）
- 中國：江南（12次）、烏日（12次）、長坪（17次）、東莞長坪（8次）、珠海（8次）
- 其他：法蘭克福（17次）、Tijuana（19次）、多倫多（16次）、拉斯維加斯（9次）、墨西哥（11次）

這些都是節目提過但沒建地點頁的城市/區域。可以：
a) 批次用 `enrich_concepts.py` 或手動建立地點頁
b) 或先接受「孤立連結」存在，不是斷連而是缺頁

### Step 2：補充地點特色
```bash
python -X utf8 tools/enrich_places.py
```

### Step 3：相關概念孤立術語（選做）
52 個概念相關概念仍空白：DP、Heart to Heart、Show Sex、S一次、偷時、全、全卸甲 等
→ 等新集數提到再補

---

## ⚠️ 已知問題 / 注意事項

- **1128 個連結指向不存在頁面**（其中大多是未建地點頁的城市）— 不是 bug，是知識缺口
- **此專案無 git rollback 保護**（無 Raw/），操作前謹慎
- `update_index.py` 的 `AUTO-GUESTS` marker 目前是人工寫死在索引裡，未來若嘉賓有更新需手動維護
- `enrich_contributors.py` 的相似度比對只掃 `人物/`，跨目錄同名不偵測
- PG島 無對應地點頁（索引已改為純文字，若之後新增了地點頁記得補回連結）

---

## 快速指令

| 指令 | 效果 |
|------|------|
| `python -X utf8 tools/ingest_ollama.py` | Step1：逐字稿 → 來源頁 |
| `python -X utf8 tools/enrich_contributors.py` | Step2：補人物頁 |
| `python -X utf8 tools/enrich_concepts.py` | Step3：補概念/店家/地點頁 |
| `python -X utf8 tools/enrich_places.py` | Step4：補地點特色 |
| `python -X utf8 tools/merge_aliases.py` | Step5：合併別名、修正連結 |
| `python -X utf8 tools/enrich_related_concepts.py --phase2` | 補相關概念（共現） |
| `python -X utf8 tools/generate_category_indexes.py` | Step7：重新生成人物/地點/店家/概念索引 |
| ⚠️ 手動補 `Wiki/來源/肥宅老司機-集數索引.md` | Step7.5：新集數標題補進集數索引 |
| `python -X utf8 tools/update_index.py` | Step8：更新主索引數字 |
| `python -X utf8 -u tools/enrich_concept_defs.py` | 選用：更新概念定義 |

---

## 知識庫現況（2026-05-19）

| 類型 | 數量 |
|------|------|
| 來源 | 290（257 集 S3EP + 33 主題/指南）|
| 概念 | 670（改名合併後）|
| 人物 | 341 |
| 店家 | 529 |
| 地點 | 47 |

---

## Ollama 別名規律備忘

1. 英中混用：Scott ↔ 史考特、James ↔ 詹姆士
2. 截斷：詹姆士 → 詹姆
3. 同音字：喇賽/喇塞/啦塞/拉塞
4. 錯別字：小瓢蟲/小飄蟲、永登埔/永登浦
5. 台/臺異體：台灣/臺灣
6. 大陸/台灣用詞：老撾 ↔ 寮國、萬象 ↔ 永珍
7. 括號變體：Cherry ↔ Cherry(櫻桃)
8. 尊稱後綴：老馬 ↔ 老馬哥（同一人）⚠️ 但老張 ≠ 老張哥（不同人）
9. 音近誤寫：老基 = 老雞、K-FUN = K房、小書桶 = 小書童
10. 同店不同名：伊林龍筋 = 伊林農金店 = 一一老師龍筋按摩 = 一一（已寫入 ALIAS_MAP）
11. 越南旗袍店：奧代/澳代 = 奧黛（áo dài）
12. 曼谷蛇美咖啡：舍美/捨美 = 蛇美
13. 城市音近：芭達雅 = 芭提雅（Pattaya）
14. 術語誤字：口報/口報店 = 口爆/口爆店；外國人料理 = 外國人料金
15. 概念合併：龍經 = 龍筋（已合併）
16. 主持人同音字：老師 = 老濕（已合併）
17. 複合詞衍生：龍經店 = 龍筋店（merge_aliases 已自動處理後綴衍生）
